PREFIX dbr: <http://dbpedia.org/resource/>
PREFIX dbo: <http://dbpedia.org/ontology/>

SELECT ?country ?label WHERE {
  VALUES ?s { dbr:Berlin }
  ?s dbo:country ?country .
  BIND("capitalish" AS ?label)
}
