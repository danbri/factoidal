# F* extracted artifacts (regenerated by `make extract`).
Spec.ml
Container_Array.ml
Test.ml

# F* checked-file caches.
*.checked
*.checked.lax
*.verified

# OCaml build artifacts.
*.cmi
*.cmo
*.cmx
*.o
*.a

# Demo binary (rebuild via `make demo`).
demo
