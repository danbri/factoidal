# Lean 4 port: what is still absent

Generated 2026-08-23 by comparing `formal/fstar/*.fst` (220 modules)
against `formal/lean4/L4Factoidal/**/*.lean` (286 modules), with an
explicit alias table for the modules that were renamed on the way
across (`Parser.XML` to `XML.Parser`, `RDF.Term` to `RDF.Core`, and
so on). The script is `tools/lean-port-gap.py`.

## Summary (generated -- do not hand-edit)

| Kind | Modules | F\* lines |
|---|---|---|
| Engine and specification code — to port | 10 | 18020 |
| Proofs about the F\* implementation — see below | 4 | 9479 |
| F\*-only machinery with no Lean counterpart by design | 14 | 6340 |
| **Total not covered** | **28** | **33839** |

192 of 220 F\* modules have a Lean counterpart.

The table above is GENERATED by `tools/lean-port-gap.py`. It used to be
hand-typed, and it drifted three times in one session on 2026-08-23 —
once per batch of ports, because every landing meant editing four
numbers by hand and the classification was never recomputed. Generating
it is the fix.

## Measurement corrections, 2026-08-23

The coverage figure moved several times in one day, and three of those
moves came from a wrong AUDIT rather than from landed code. Each
correction is recorded here with the method that produced the wrong
answer, because the next reader needs to know which number they are
looking at.

### First: module-name matching, and what it cannot see

`DID.Key` turned up by hand as covered by `L4Factoidal/VC/DidKey.lean`
all along. I audited the rest of the not-covered list by squashed MODULE
NAME, found nothing more, and wrote "an audit of the whole not-covered
list found no other false negative".

That method cannot see two things, and both were present:

1. **A consolidation** — one Lean module covering two F\* modules under a
   third name. `L4Factoidal/RDF/Entailment.lean` covers BOTH
   `RDF.Entailment.Simple` (182) and `RDF.Entailment.Regime` (271); its
   own header says so in its first sentence.
2. **A substantive rename** — `Parser.CSVResults` (610) became
   `SPARQL.ResultsCsvTsv`.

A definition-name audit — every `let` / `val` / `type` of each
not-covered F\* module against every `def` / `abbrev` / `structure` /
`inductive` / `theorem` in the Lean tree, normalised for case and
underscores — found those.

### Second: the method that was in the tree all along

**A Lean module's header names the F\* module it ports.** Extracting
every `formal/fstar/X.fst` mention from every Lean header, and reading
the sentence around each, settles the question directly. That found five
more genuinely covered engine modules, 3,015 F\* lines:

| F\* module | Lines | Lean module, and what its header says |
|---|---|---|
| `RDF.Entailment.RDFS.RhoDFClosure` | 1996 | `RDFS/RdfsCore.lean` — "Ports the rule set of" |
| `RDF.IRI` | 530 | `Syntax/IriResolve.lean` — "Port of" |
| `Parser.SRX` | 291 | `SPARQL/ResultsXml.lean` — "Port of … (parsing)" |
| `Parser.JSONResults` | 160 | `SPARQL/ResultsJson.lean` — "Port of … (parsing)" |
| `SPARQL11.IRI.Resolve` | 38 | `Syntax/IriResolve.lean` — both F\* modules delegate to one core |

⚠️ Two of those claims are PARTIAL: `RdfsCore.lean` ports the RULE SET,
not `RhoDFClosure`'s five theorems, and the two Results modules port the
PARSING halves plus the serialising halves of `SPARQL.Protocol.fst`, not
everything in the F\* files.

Reading the headers also settled two the other way, which a ratio-based
audit would have counted as near-misses:

- `SPARQL.FullText` (223) is NOT covered, and `SPARQL/Parser.lean` says
  so: "whose encoding lives in `SPARQL.FullText.fst` — no Lean".
- `JSONLD.Frame` (335) is NOT covered. `JSONLD/Expand.lean` merely lists
  it beside `Compact.fst` and `Flatten.fst` as a sibling.

`Parser.JSONLD` (1459) is claimed only as "Port of the **toRdf half**"
by `JSONLD/ToRdf.lean`. The expand, compact and flatten halves have
their own Lean modules; whether the union is complete has not been
checked, so it stays in the not-covered column rather than being counted
on a guess.

### One narrow behavioural gap inside the coverage

The F\* `match_term` takes a POSITION-AWARE literal comparison,
`leq : inside_tt -> literal -> literal -> bool`, because a directional
language string is opaque — compared case-sensitively — only INSIDE a
triple term. Lean's `entailsWith` takes `leq : Literal -> Literal ->
Bool` with no position flag. The coverage is substantive but not
complete, and the difference is what the W3C
`opaque-dir-language-string` fixtures exercise.

### The rule

An audit that finds nothing is evidence about the AUDIT's reach before
it is evidence about the code. State the method next to the result, and
pick one that can see the failure you are looking for. Full text: hazard
#28 in `skills/workflow-gotchas-debugging`, anti-pattern #28 in
CLAUDE.md.

### On the proof column

These are F\* modules whose content is a proof about the F\*
implementation: `.Spec`, `.Refinement`, `.ModelTheory`,
`.Completeness`, `.Axioms`, `.RoundTrip`. The Lean tree carries its
own theorem modules (`*Theorems.lean`, `RLTheorems`,
`TableauTheorems`, `SyntaxTheorems`, and the `#guard` layer), so a
module-for-module port is not the right measure of this column. What
IS the right measure — which obligations the F\* discharges that the
Lean does not — has not been computed. Recording that as unknown
rather than as zero.

### On the F\*-only column

`Parser.FastString.*`, seven modules. The F\* parser indexes raw
UTF-8 bytes, so it needs machinery to rebuild codepoints from bytes
and reject invalid sequences. Lean's `String` and `Char` are
codepoint types and `Char` is a valid scalar value by construction,
so that machinery has no counterpart. `L4Factoidal/XML/Document.lean`
states this in its header.

`RDF.Indexed.KeyInjectivity` (963 lines), `RDF.Indexed.Completeness`
(651), `RDF.Entailment.RDFS.SepFree` (697) and
`RDF.Entailment.RDFS.ChainWf` (368) — 2,679 lines — were reclassified
into this column on 2026-08-23. All four repair ONE property of the
F\* index: its bucket key is built by concatenating strings with a
U+001F separator, and that key is not injective, because `is_iri`
admits U+001F. `RDF.Semantics.HypothesisWitness.theorem_sp_key_not_injective`
exhibits the collision. The four modules are the repair:

- `KeyInjectivity` proves the key injective as soon as ONE side is
  free of U+001F, and discharges `ig_wf_sp (build_indexed g)` from the
  per-graph side condition `graph_sp_sep_free g`.
- `SepFree` proves, row by row, that every RDF and RDFS closure rule
  sends a U+001F-free graph to a U+001F-free graph, because a
  one-graph discharge does not carry through a closure CHAIN.
- `ChainWf` folds those rows into one step lemma and inducts it over
  `closure_iter`, giving `closure_chain_wf g` from `graph_sep_free g`.
- `RDF.Indexed.Completeness` proves the coverage direction — every
  triple of `g` with predicate `p` IS served by `p`'s bucket — through
  three interface axioms about `FStar.String.compare`, because the
  keys are strings and the buckets are sorted by string order.

None of that obligation exists in the Lean tree.
`L4Factoidal/OWL/RLClosureIndexed.lean` keys its five buckets on
STRUCTURED values — `Subject`, `WfIri`, `Subject × WfIri`,
`WfIri × Term` — in a `Std.HashMap`. There is no separator character,
so there is no collision to exclude, no side condition to propagate,
and no string-order axiom to assume. The well-formedness statement is

    def BucketWf {κ : Type} [BEq κ] [Hashable κ]
        (m : Std.HashMap κ (List Triple)) (key : Triple → κ) (g : Graph) : Prop :=
      ∀ k, (m.getD k []).reverse = g.filter (fun u => key u == k)

and `Index.Wf.ofGraph (g : Graph) : Wf (ofGraph g) g` holds for EVERY
graph, with no hypothesis. `Wf.withSubjPred_eq` and its four siblings
then give both directions at once — the bucket lookup EQUALS the list
filter, so soundness and completeness are the same equation — and
`closureI_toGraph` lifts it to a per-fuel list equality between the
indexed closure and the list closure. The Lean statement is stronger
than the F\* one and carries no side condition, so a module-for-module
port of the four repair modules would produce four Lean files that
prove nothing the tree does not already have.

`OWL.Semantics.MemLemmas` (442 lines) joined them on 2026-08-23, for
the same reason one level up. About half of it IS that index machinery —
`tree_ok`, `lemma_tree_ok_lookup`, `lemma_slt_tree_ok`,
`lemma_build_bucket_ok`, and five `lemma_build_indexed_wf_*`
instantiations. The other half is lemmas about `List.Tot.sortWith`,
`partition` and `rev`, which exist ONLY because the `bucket_tree` build
sorts and partitions. The Lean index does neither, so there is nothing
for a membership-preservation lemma to preserve through, and
`OWL/RLTheorems.lean` proves the same OWL RL soundness results with none
of it.

`RDF.CottasStore.ColumnSeq` (163 lines) joined on the same day for a
different absence. It is `assume new type cottas_column` plus O(1)
accessors, realised in OCaml as `string option array`, with a list
bridge. Its own banner gives the reason: the F\*-pure decoders produce
`list (option string)`, every walk cons-cell-chases through the heap,
and F\* needs a comparable array shape to retire the OCaml perf shim.
Lean has `Array` natively and totally — `Array.size`, `arr[i]?` and
`Array.toList` are the entire module, with no abstract type and no
assumed accessor.

Two consequences a later reader needs:

1. The gap in `L4Factoidal/RDF/SemanticsHypothesisWitness.lean` § 4 —
   "no witness for `ig_wf_sp` or `closure_chain_wf`" — is a gap in the
   F\* tree only. In Lean those hypotheses are absent from the
   statements, so there is nothing to witness.
2. This is a claim about the CURRENT Lean structures. A serialised or
   string-keyed index brings the first five back; a decoder that needs
   an abstract column handle brings `ColumnSeq` back. Tracked at
   <https://github.com/danbri/factoidal/issues/559>.


### Third: the tool read a cache instead of the repository

`tools/lean-port-gap.py` took both module lists from two text files in
the session scratchpad. The F\* list was stable, but the Lean list was a
snapshot, and it was written before the last ports. So a freshly landed
module — `RDF.CottasStore.CompoundPresenceWriter` — was reported as NOT
COVERED by the tool that exists to answer exactly that question, while
its Lean file sat in the tree.

The scratchpad is per-session and is deleted with the container, so the
same tool would also have crashed on a fresh session rather than
reporting a stale answer. Both failures have one cause: a measurement
tool whose input a person has to remember to refresh.

The tool now walks `formal/fstar/*.fst` and
`formal/lean4/L4Factoidal/**/*.lean` on every run and exits non-zero if
either walk comes back empty. There is no cache left to go stale.


### Fourth: a name-similarity rule was inflating the count by five

The tool counted an F\* module as covered when an alias matched, when
the last TWO name components matched, OR when the last ONE did. The
last rule is wrong, and it had been wrong for the whole session.

Adding `L4Factoidal/HDT/Store.lean` made `SPARQL11.Store` — 1,452
lines, unported — vanish from the not-covered list, because both names
end in `Store`. One module landed and the count rose by two; the second
increment was a module nobody had touched. That is the signal that
found it.

The audit of all fourteen bare-leaf matches found seven wrong:

| F\* module | matched | verdict |
|---|---|---|
| `SPARQL11.Store` | `HDT.Store` | wrong |
| `RIF.Core.Tests` | any of fifteen `*.Tests` | wrong, now aliased to `RIF.EngineTests` |
| `RDF.Store.Loader` | `JSONLD.Loader` | wrong |
| `Math.Expr` | `SPARQL.Expr` | wrong |
| `SPARQL.Protocol.Client` | `HTTP.Client` | wrong |
| `RDF.Turtle.Serialize` | `JSON.Serialize` | wrong — no Lean counterpart exists |
| `RDF.NQuads.Serialize` | `JSON.Serialize` | wrong match, but `Syntax.NQuads` does port it |

And two aliases pointed at `RDF.Serialize`, a Lean module that DOES NOT
EXIST. The alias silently did nothing and the leaf rule matched
`JSON.Serialize` instead, so the breakage was invisible: a lookup table
whose misses fall through to a heuristic cannot report its own
failures.

The tool now counts only an alias or a two-component match, and prints
every alias whose target is absent before the report. Everything
genuine that had relied on a bare leaf match is an explicit alias,
each verified against the Lean module's own `Port of
formal/fstar/<X>.fst` line — except the three RIF modules, which carry
no such line and were verified by subject matter, the weaker evidence
class, labelled as such in the table.

Corrected figure: the count reported through this session as 156 was
**151**; with `Parser.BallyhooHDT` landing alongside the fix it is 153.

### Fifth: a port that was written, then deleted as a duplicate

`Math.Expr` was ported in full (510 lines) and built on its own. The
FULL-TREE build then failed with `environment already contains
'L4Factoidal.Math.mAdd'`, which is how it emerged that
`L4Factoidal/MathML/Core.lean` already carries the same maths core —
the same five-constructor AST, the same exact rational arithmetic, the
same exact roots, factorial and `eval` — inside the MathML namespace,
and that `Math/Simplify.lean` carries a third arithmetic beside it.

The new file was DELETED, not landed. `Math.Expr` is now an alias onto
`MathML.Core` marked PARTIAL, naming the two pieces genuinely absent
from the Lean tree: `parse_decimal`, and the reasoned `MV_Undef` value
(`MathML.Core` returns a bare `Option`, so a caller cannot tell "not a
number" from "divided by zero"). The layering inversion and its fix
are <https://github.com/danbri/factoidal/issues/557>.

This is why the count went from 155 to 156 with no new Lean file: the
coverage was already there, mislaid under another namespace.


### Sixth: four RENAMED ports were missing from the alias table

The fourth correction removed bare-leaf matching, which was inflating
the count. This one is the opposite error, found by the opposite method.

Every module on the not-covered list was searched for by NAME inside the
Lean tree — `grep -rl '<Module>.fst' formal/lean4/L4Factoidal`. Seven
modules turned up a mention. Each was then read in context, and four of
the seven were ports whose Lean file says so in its own header:

| F\* module | Lean counterpart | What the header says |
|---|---|---|
| `Parser.JSONLD` | `JSONLD.ToRdf` | "Port of the toRdf half of `Parser.JSONLD.fst`" |
| `RDF.CottasStore.PageCache.Bounds` | `Cottas.PageCache` | "Port of `RDF.CottasStore.PageCache.Bounds.fst` (463 lines)" |
| `SPARQL.Protocol.RoundTrip` | `SPARQL.ResultsTheorems` | proves "the same OBSERVABLE statement" as `lemma_srj_n_rows` |
| `OWL.RL.Refinement` | `OWL.RLTheorems` | "This is the LICENSING theorem `OWL.RL.Refinement.fst` states", one lemma per row |

The other three mentions were NOT ports, and are deliberately still
counted as not covered:

- `OWL.Semantics.Soundness` — `OWL/RLTheorems.lean` names it under its
  own "What is NOT proved" heading. Truth preservation is not ported.
- `RDF.Entailment.RDFS.Completeness` — `RDFS/RdfsCore.lean` cites it in
  prose. The F\* module proves MODEL-THEORETIC completeness through the
  Herbrand interpretation of a closed graph; the Lean tree's
  `complete_of_saturated` is the syntactic statement. Different
  theorems.
- The `Parser.JSONLD` mention in `JSON/Serialize.lean` is a reference to
  its writer shape, not a port claim; `JSONLD.ToRdf` is the port.

Both directions of this error have now bitten in one session — a
matching rule too loose, then an alias table too sparse. The method that
found this one is cheap and repeatable, so run it before quoting the
number: search the tree for each not-covered module's file name, then
READ the hit rather than counting it.

### Seventh: a name-level overlap that was NOT a false negative

`SPARQL11.Expression.Refinement` looked like the next alias-table gap.
`SPARQL/ExprTheorems.lean` carries the same two section headings the F\*
module is about — §17.2.2 effective boolean value, §17.3 connective
truth tables — and proves the same rows. The cheap check would have
aliased it and moved the count by one without any code.

Reading the F\* header settled it the other way. That module states its
own job as one independent spec transcription per operator plus one
lemma tying the engine's arm to it. `ExprTheorems` states its rows about
the engine's `ebv`/`boolAnd`/`boolOr`/`boolNot` directly; it carries no
second transcription, so it cannot do that job. The port was written
(`SPARQL/ExprRefinement.lean`) and the alias points at it.

The check that works in both directions is the same one: read the header
in context. It removed four false negatives in the sixth correction and
prevented a false positive here.

### Eighth: a port that could not be stated until the engine was fixed

`RDF.Entailment.Simple.Refinement` proves the shipping search complete
for the specification. Porting it exposed that the Lean `matchObject`
was NOT complete: its object arm bound a top-level blank node but fell
to `termMatch` for an RDF 1.2 triple term, and `termMatch` compares a
triple term's subject and interior object by identity. So
`a p <<( a p _:b )>>` matched only a premise carrying the same interior
label, and `simpleEntails` answered `false` on a pair the substitution
`_:b` to `c` relates. The F\* `match_term` always recursed; the port had
dropped the recursion.

This is a fourth way the coverage number can mislead, next to the three
already recorded. A module can be absent because the theorem it states
is FALSE of the port — not because nobody has written it yet. Counting
it as "to port" was right; treating the port as transcription work
would have been wrong.

The engine arm now recurses, and
`entailsSimple_tripleTerm_interior_bnode` pins the witness that used to
decide `false`.

### Ninth: a partial port that does NOT move the count

`RDF.NTriples.RoundTrip` (1504 lines) got a partial port —
`Syntax/NTriplesRoundTrip.lean` carries the serialiser-injectivity half
and the print-safe IRI fragment — and is still counted as NOT covered.

The F\* source reaches an IRI round trip through a character-list
detour. The Lean module does not, and the reason is worth separating
from the F\* one. F\*'s obstacle is that its scanner reads bytes
through `assume val` primitives with no base-value equations, so no
branch fires for any input. Lean's parser has no such obstacle: it
reduces, it runs, and the module's `#guard`s execute it. What stops the
theorem is that `readIriRefBody` has ten match arms, two carrying six-
and ten-character escape patterns, and generating Lean's per-arm
equation lemmas for it exhausts the container's memory —
`#check @readIriRefBody.eq_11`, alone in a file, is killed by the OOM
killer before any proof is attempted.

Filed as <https://github.com/danbri/factoidal/issues/565> with the
scanner refactor that unblocks it.

The rule this sets: a module counts as covered when the Lean side
carries the F\* module's result, not when a file with a matching name
exists. Aliasing this one would have moved the number by one and made
it mean less.

**The rule applied a second time, deliberately.**
`SPARQL/AlgebraRefinement.lean` is layer 1 of the port of
`SPARQL11.Algebra.Refinement` (2497 lines): UNION at both layers,
FILTER at both layers under the congruence hypothesis the F\* source
also names, and the compatibility bridge with its witness. JOIN,
LEFTJOIN, EXTEND, PROJECT, DISTINCT and the BGP vertical are not in it.
No alias was added, and `SPARQL11.Algebra.Refinement` stays on the
not-covered list. The count did not move, and that is the correct
outcome rather than a failure — the ninth and tenth corrections
together say a still count and a moving count are each only as good as
the reason you can give for them.

### Tenth: the alias edit that was not there

`RDF.Entailment.RDFS.Completeness` was ported and the alias added. The
count did not move: 191 before, 191 after.

Rule 2 of `skills/counting-coverage` says a count that moves with no
cause is a bug report. The converse holds too, and this is the case
that proves it: a count that does NOT move when a landing should have
moved it is the same warning. A `git stash` / `git stash pop` cycle run
between two measurements had dropped the edit to
`tools/lean-port-gap.py`. The Lean file was on disk, verified and
building; the tool had never heard of it.

Two things would have hidden this. The alias table cannot report its
own gaps (rule 4) — a missing entry is silent by construction. And the
publishable-looking number was correct for a DIFFERENT reason: 191 was
the true figure before this landing, so nothing on screen looked wrong.

The check that caught it costs one command: `grep` the tool for the
alias you just added, before reading its output. That is now the first
item after any alias edit.

## Not covered, by group

| Group | Modules | F* lines |
|---|---|---|
| `RDF.*` | 12 | 13823 |
| `OWL.*` | 4 | 6157 |
| `Parser.*` | 8 | 5709 |
| `SPARQL11.*` | 3 | 4801 |
| `Parquet.*` | 1 | 3349 |
| **Total** | **28** | **33839** |

### RDF — 12 modules, 13823 lines

- `RDF.CottasStore` (2825 lines)
- `RDF.CottasStore.BaseWriter` (1282 lines)
- `RDF.CottasStore.ColumnSeq` (163 lines)
- `RDF.Entailment.RDFS.ChainWf` (368 lines)
- `RDF.Entailment.RDFS.Refinement` (1613 lines)
- `RDF.Entailment.RDFS.SepFree` (697 lines)
- `RDF.Indexed.Completeness` (651 lines)
- `RDF.Indexed.KeyInjectivity` (963 lines)
- `RDF.List.Helpers` (195 lines)
- `RDF.NQuads.Streaming` (3438 lines)
- `RDF.NTriples.RoundTrip` (1504 lines)
- `RDF.Store.Capabilities.Cottas` (124 lines)

### OWL — 4 modules, 6157 lines

- `OWL.QueryEval` (51 lines)
- `OWL.QueryRewrite` (1799 lines)
- `OWL.Semantics.MemLemmas` (442 lines)
- `OWL.Semantics.Soundness` (3865 lines)

### Parser — 8 modules, 5709 lines

- `Parser.FastString` (351 lines)
- `Parser.FastString.Axioms` (351 lines)
- `Parser.FastString.BaseCases` (296 lines)
- `Parser.FastString.CharBoundary` (57 lines)
- `Parser.FastString.ConcatSpec` (154 lines)
- `Parser.FastString.RoundTripLemmas` (800 lines)
- `Parser.FastString.Spec` (852 lines)
- `Parser.NTriples.Locality` (2848 lines)

### SPARQL11 — 3 modules, 4801 lines

- `SPARQL11.Algebra.Refinement` (2497 lines)
- `SPARQL11.Parser.AskBgpRoundTrip` (852 lines)
- `SPARQL11.Store` (1452 lines)

### Parquet — 1 modules, 3349 lines

- `Parquet.Footer` (3349 lines)
