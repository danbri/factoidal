/-
Harness.RdfSemanticsRun — the W3C rdf-semantics manifest
(`lake exe l4rdf-semantics`). Placeholder: the walk lands next.
-/
import Harness.Fixtures

def main (_args : List String) : IO UInt32 := do
  IO.println "rdf-semantics: not implemented yet"
  return 0
