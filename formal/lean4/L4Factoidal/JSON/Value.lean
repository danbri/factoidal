/-
L4Factoidal.JSON.Value — WIP checkpoint, not yet building.

Port target: `formal/fstar/Parser.JSON.fst`'s `json_val` type and its
accessors (`json_get_field` / `json_get_string` / ...). This file is
being written; see the branch `lean4/json` for progress.
-/
