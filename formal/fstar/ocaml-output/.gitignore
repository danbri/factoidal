# OCaml compilation artifacts (platform-specific, rebuilt by ../build-ocaml.sh)
*.cmi
*.cmx
*.cmo
*.o
*.a
*.cmxa
*.cma
*.byte
*.js.map

# Native executables are now in bin/<platform>/ at repo root.
# Symlinks here point to the current platform's binaries.
w3c_runner
factoidal

# js_of_ocaml output
*.bc.js

# Build transcripts (regenerated every build; not rule-#9 artifacts).
# Result logs (*_results.log, scraped by generate-report.sh) stay TRACKED.
_*.log
