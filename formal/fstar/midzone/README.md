# midzone

Small, explicit choices that are reasonable but debatable:
dataset meaning, chosen datatype maps, IRI validation strictness, entailment
regime selection, graph canonicalisation strategy, and project-local readings
where a standard leaves room.

The goal is to keep these choices visible instead of smuggling them into the
core model or the executable engine.
