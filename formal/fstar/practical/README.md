# practical

Implementation details: list-backed graphs, parser state, byte-oriented
string primitives, extraction boundaries, indexes, storage backends, protocol
server plumbing, W3C runner integration, and performance-oriented helpers.

This bucket may depend on `corespecs/` and `midzone/`, but the reverse should
be rare and treated as a design smell.
